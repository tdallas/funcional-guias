h x y = f (g x y)
h es una funcion que recibe dos parametros y devuelve una funcion que toma como parametro el resultado de aplicar g con los dos parametros que recibe h
h = f . g
h x y = (f . g) x y 